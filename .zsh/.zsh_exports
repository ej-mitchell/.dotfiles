# Oh My ZSH
export ZSH=/Users/tb_emitchell/.oh-my-zsh

# Postgres
# Note: This is left over from when dev environment depended on PG; might need to be updated in the future

export PGHOST=localhost

# Editor
export EDITOR=nvim

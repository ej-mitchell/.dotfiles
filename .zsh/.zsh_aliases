# Unix
alias lsa="ls -a"
alias ll="ls -al"
alias ln="ln -v"
alias mkdir="mkdir -p"

# Bundler
alias b="bundle"

# Rails
alias migrate="rake db:migrate db:rollback && rake db:migrate db:test:prepare"
alias s="rspec"

# `vim` => `nvim`
alias vim="nvim"

# Epic Games

alias hpsdk="cd ~/Documents/dev/houseparty-sdk"
alias hpweb="cd ~/Documents/dev/houseparty-web-client"
